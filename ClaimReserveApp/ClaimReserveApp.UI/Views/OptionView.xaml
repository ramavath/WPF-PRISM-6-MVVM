<UserControl x:Class="ClaimReserveApp.UI.Views.OptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClaimReserveApp.UI.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Name="ClaimResevedButton" Command="{Binding NavigateCommand}" Template="{StaticResource SubmitButtonTemplate}" CommandParameter="ReserveSummaryView" Cursor="Hand">Claims Reserved  </Button>
            <Button Name="DevelopmentButton" Command="{Binding NavigateCommand}" Template="{StaticResource SubmitButtonTemplate}" CommandParameter="DevelopmentSummaryView" Cursor="Hand">Development Report</Button>
        </StackPanel>
    </Grid>
</UserControl>
